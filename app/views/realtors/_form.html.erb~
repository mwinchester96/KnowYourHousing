<%= form_for(@realtor) do |f| %>
  <% if @realtor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@realtor.errors.count, "error") %> prohibited this realtor from being saved:</h2>

      <ul>
      <% @realtor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :address %><br>
    <%= f.text_field :address %>
  </div>
  <div class="field">
    <%= f.label :college%><br>
    <%= f.select :college, options_for_select(["Wesleyan University", "UMass Amherst", "Williams College", "University of Connecticut", "Amherst College", "University of New Hampshire","Keene State College","Plymouth State College","Westfield State University"]) %>
  </div>
  <div class="field" style="padding-bottom:10px;">
    <%= f.label :Website %><br>
    <%= f.text_field :realtor_url %>
  </div>
  
  <div class="actions">
    <%= f.submit 'Save', :class => 'btn btn-primary'%>
  </div>
<% end %>
